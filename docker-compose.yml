services:
  postgres:
    image: postgres:16-alpine
    container_name: dryer-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: dryerdb
      POSTGRES_USER: dryer
      POSTGRES_PASSWORD: dryer123
    ports:
      - "55432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./docker/init.sql:/docker-entrypoint-initdb.d/01-init.sql

volumes:
  pgdata:
